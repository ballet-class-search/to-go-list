const DB_NAME="princess-to-go-list",DB_VERSION=1,STORE_NAME="spots",LEGACY_KEY="want-to-go-spots-v1";const sampleSpots=[];function openDatabase(){return new Promise((resolve,reject)=>{const request=indexedDB.open(DB_NAME,DB_VERSION);request.onupgradeneeded=()=>{const db=request.result;if(!db.objectStoreNames.contains(STORE_NAME))db.createObjectStore(STORE_NAME,{keyPath:"id"})};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}function requestResult(request){return new Promise((resolve,reject)=>{request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}function transactionDone(transaction){return new Promise((resolve,reject)=>{transaction.oncomplete=()=>resolve();transaction.onerror=()=>reject(transaction.error);transaction.onabort=()=>reject(transaction.error)})}function migrateSpot(spot){const priorityMap={高:5,中:3,低:1},statusMap={今度行く:"気になる",また行きたい:"行った"},areas=[...new Set((Array.isArray(spot.areas)?spot.areas:[spot.area]).map(area=>String(area||"").trim()).filter(Boolean))];return{...spot,areas,area:undefined,photos:Array.isArray(spot.photos)?spot.photos.map(photo=>photo&&typeof photo==="object"&&"source"in photo?{source:photo.source,positionX:Number.isFinite(photo.positionX)?photo.positionX:50,positionY:Number.isFinite(photo.positionY)?photo.positionY:50,zoom:Number.isFinite(photo.zoom)?photo.zoom:1}:{source:photo,positionX:50,positionY:50,zoom:1}):[],links:Array.isArray(spot.links)?spot.links:spot.url?[{label:"関連リンク",url:spot.url}]:[],priority:typeof spot.priority==="number"?spot.priority:priorityMap[spot.priority]||3,status:statusMap[spot.status]||spot.status||"気になる"}}async function seedOrMigrate(db){const count=await requestResult(db.transaction(STORE_NAME).objectStore(STORE_NAME).count());if(count>0)return;let initial=sampleSpots;try{const legacy=JSON.parse(localStorage.getItem(LEGACY_KEY)||"null");if(Array.isArray(legacy)&&legacy.length)initial=legacy.map(migrateSpot)}catch{}const transaction=db.transaction(STORE_NAME,"readwrite");initial.forEach(spot=>transaction.objectStore(STORE_NAME).put(migrateSpot(spot)));await transactionDone(transaction)}export async function loadSpots(){const db=await openDatabase();await seedOrMigrate(db);const spots=await requestResult(db.transaction(STORE_NAME).objectStore(STORE_NAME).getAll());return spots.map(migrateSpot)}export async function saveSpot(spot){const db=await openDatabase(),transaction=db.transaction(STORE_NAME,"readwrite");transaction.objectStore(STORE_NAME).put(migrateSpot(spot));await transactionDone(transaction)}export async function deleteSpot(id){const db=await openDatabase(),transaction=db.transaction(STORE_NAME,"readwrite");transaction.objectStore(STORE_NAME).delete(id);await transactionDone(transaction)}